SET(FBRENDER_SRCLIST
		main.cpp)

SET(LIBRARIES  libfbrender)

ADD_EXECUTABLE(fbrender ${FBRENDER_SRCLIST})

TARGET_LINK_LIBRARIES(fbrender ${LIBRARIES})
INSTALL(TARGETS fbrender RUNTIME DESTINATION ${CMAKE_INSTALL_PREFIX}/bin)

